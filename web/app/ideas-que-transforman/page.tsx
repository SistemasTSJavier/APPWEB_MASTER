import { IdeasPublicClient } from "./IdeasPublicClient";

export const dynamic = "force-dynamic";

/** Formulario público (QR): proponer una mejora sin iniciar sesión. */
export default function IdeasQueTransformanPublicPage() {
  return <IdeasPublicClient />;
}
