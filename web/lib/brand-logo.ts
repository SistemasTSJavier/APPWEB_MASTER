/** Logo general de la app (WebP). */
export const BRAND_LOGO_SRC = "/logo.webp";

export const BRAND_LOGO_FALLBACKS = ["/logo.webp", "/logo-tactical-support.png"] as const;

/** Solo dashboard de categorización (pantalla completa / export). */
export const CAT_DASHBOARD_LOGO_FALLBACKS = ["/logo.png"] as const;
